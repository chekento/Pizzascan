'use strict';
const DRAFTS='pizzascan-drafts-v1';
function showDraft(source){
 const venue=source?.place||source;if(!venue?.placeId)return toast('Bitte zuerst ein Restaurant auswählen');
 detailRequest?.abort();
 const R=PizzaReview,stored=readDraftRecords()[venue.placeId];
 let state=R.normalize(stored?.state||{own:source.own,notes:source.notes,visited:false});
 let manual=stored?.manual===true,pending=false,draft=manual&&typeof stored?.text==='string'?stored.text:R.generate(state);
 function storeDraft(){const all=readDraftRecords(),entry={place:venue,state,text:draft,manual,updatedAt:new Date().toISOString()},filled=PizzaDrafts.hasContent(entry);if(filled)all[venue.placeId]=entry;else delete all[venue.placeId];const ok=placeService.write(DRAFTS,all);$('draft-save-state').textContent=ok?(filled?'Entwurf auf diesem Gerät gespeichert':'Deine Eingaben werden automatisch gespeichert'):'Speicher voll: Bitte Text kopieren, bevor du schließt.';renderDraftBadge();return ok;}
 const choices=(items,attr,current)=>items.map(c=>`<button type="button" class="builder-choice" ${attr}="${esc(c.id)}" aria-pressed="${current===c.id}">${esc(c.label)}</button>`).join('');
 openSheet('draft','GOOGLE MAPS · REZENSIONSBAUKASTEN',`<h1>So war dein Besuch.</h1><p class="review-venue">${esc(venue.name)}</p><p class="hint">Wähle, was du selbst erlebt hast. Jeder Baustein ist freiwillig. Der Entwurf bleibt privat, bis du ihn selbst veröffentlichst.</p><div class="builder-modes" role="group" aria-label="Art des Besuchs">${choices(Object.entries(R.modes).map(([id,label])=>({id,label})),'data-visit-mode',state.mode)}</div><p id="review-mode-hint" class="hint">Wähle deine Besuchsart, damit passende Aspekte erscheinen.</p><div class="builder-rating"><label for="review-rating">Deine Gesamtbewertung / 10</label><input id="review-rating" type="number" min="0.1" max="10" step="0.1" inputmode="decimal" placeholder="0,1–10,0" value="${state.own??''}"><p id="review-stars" class="hint"></p></div><details class="builder-more"><summary>Bestellung ergänzen</summary><label for="review-dish">Was hast du bestellt?</label><input id="review-dish" maxlength="100" placeholder="Zum Beispiel Pizza Margherita" value="${esc(state.dish)}"></details><h2>Deine Eindrücke</h2><p class="hint">Antippen zum Einfügen, erneut antippen zum Entfernen.</p><div id="review-aspects"></div><div class="field"><label for="review-notes">Eigene Ergänzung</label><textarea id="review-notes" maxlength="1500" placeholder="Was möchtest du noch erzählen?">${esc(state.notes)}</textarea></div><label for="review-return">Würdest du wiederkommen oder bestellen?</label><select id="review-return"><option value="">Nicht erwähnen</option><option value="yes">Ja</option><option value="maybe">Noch unentschieden</option><option value="no">Eher nicht</option></select><div class="builder-format"><div><label for="review-length">Textform</label><select id="review-length"><option value="short">Kompakt</option><option value="standard">Fließtext</option><option value="detailed">Mit Absätzen</option></select></div><div><label for="review-tone">Schreibstil</label><select id="review-tone"><option value="personal">Persönlich</option><option value="factual">Sachlich</option></select></div></div><div class="section-title"><h2>Dein Rezensionstext</h2><button id="review-rephrase" class="text-button">Einleitung variieren</button></div><label for="draft-text" class="hint">Prüfen und frei bearbeiten</label><textarea id="draft-text" maxlength="4000">${esc(draft)}</textarea><div id="draft-manual-note" class="notice" ${manual?'':'hidden'}>Du bearbeitest den Text selbst. Neue Bausteine überschreiben ihn erst, wenn du sie übernimmst.<button id="draft-apply" class="text-button">Text aus Bausteinen übernehmen</button></div><p class="hint"><span id="draft-count"></span> · <span id="draft-save-state" role="status"></span></p><label class="check"><input id="review-visited" type="checkbox" ${state.visited?'checked':''}><span>Ich habe den Besuch oder die Bestellung selbst erlebt und jede Aussage geprüft.</span></label><p id="draft-gate" class="hint"></p><button id="maps-draft" class="primary full">Text kopieren & Google Maps öffnen ↗</button><div class="card-actions"><button id="copy-draft" class="secondary">Nur Text kopieren</button><button id="draft-export" class="secondary">Entwurf als JSON sichern</button><button id="draft-list-back" class="secondary">Alle Entwürfe</button><button id="draft-delete" class="text-button danger">Entwurf löschen</button></div><p class="hint">In Google Maps das richtige Restaurant wählen, „Rezension schreiben“ öffnen und den Text einfügen. Sterne und Veröffentlichung bestimmst du dort. PizzaScan ist unabhängig von Google.</p>`);
 function renderAspects(){const active=document.activeElement,focus=active?.dataset.aspect?{aspect:active.dataset.aspect,choice:active.dataset.choice,detail:active.dataset.detail}:null;const visible=R.available(state.mode);$('review-aspects').innerHTML=visible.map(a=>`<fieldset class="builder-aspect"><legend>${a.icon} ${esc(a.label)}${state.selected[a.id]?'<span class="builder-check" aria-label="ausgewählt"> ✓</span>':''}</legend><div class="builder-options">${a.choices.map(c=>`<button type="button" class="builder-choice" data-aspect="${a.id}" data-choice="${c.id}" aria-pressed="${state.selected[a.id]===c.id}">${esc(c.label)}</button>`).join('')}</div>${a.followup&&state.selected[a.id]===a.followup.when?`<div class="builder-followup"><span>${esc(a.followup.label)}</span><div class="builder-options">${a.followup.choices.map(c=>`<button type="button" class="builder-choice" data-aspect="${a.id}" data-detail="${c.id}" aria-pressed="${state.details[a.id]===c.id}">${esc(c.label)}</button>`).join('')}</div></div>`:''}</fieldset>`).join('');if(focus)document.querySelector(`[data-aspect="${focus.aspect}"][${focus.detail?'data-detail':'data-choice'}="${focus.detail||focus.choice}"]`)?.focus({preventScroll:true});}
 function update(changed=true){
  if(changed){state.visited=false;$('review-visited').checked=false;if(manual)pending=true;else {draft=R.generate(state);$('draft-text').value=draft;}}
  $('review-mode-hint').hidden=!!state.mode;
  $('draft-manual-note').hidden=!manual;
  $('draft-apply').textContent=pending?'Geänderte Bausteine in den Text übernehmen':'Text aus Bausteinen übernehmen';
  $('review-stars').textContent=state.own===null?'Wähle deine eigene Bewertung von 0,1 bis 10,0.':`${state.own.toFixed(1)}/10 entsprechen gerundet ${A.stars(state.own)} von 5 Sternen. Du entscheidest in Google Maps.`;
  const valid=!!state.mode&&state.own!==null&&state.visited&&draft.trim().length>0&&draft.length<=4000;
  $('maps-draft').disabled=!valid;$('copy-draft').disabled=!valid;
  $('draft-gate').textContent=valid?'Bereit zum Kopieren.':!state.mode?'Bitte Vor Ort, Abholung oder Lieferung wählen.':state.own===null?'Bitte deine Gesamtbewertung ergänzen.':!state.visited?'Bitte deine eigene Erfahrung und den Text bestätigen.':'Bitte einen Text mit höchstens 4000 Zeichen eingeben.';
  $('draft-export').disabled=!PizzaDrafts.hasContent({state,text:draft,manual});
  $('draft-count').textContent=`${draft.length} / 4000 Zeichen`;storeDraft();
 }
 for(const id of ['return','length','tone'])$('review-'+id).value=state[id==='return'?'returnVisit':id];
 $('sheet-body').onclick=e=>{const b=e.target.closest('button');if(!b)return;if(b.dataset.visitMode){state.mode=b.dataset.visitMode;document.querySelectorAll('[data-visit-mode]').forEach(el=>el.setAttribute('aria-pressed',el.dataset.visitMode===state.mode));renderAspects();update();}else if(b.dataset.aspect){const key=b.dataset.detail?'details':'selected',value=b.dataset.detail||b.dataset.choice;state[key][b.dataset.aspect]=state[key][b.dataset.aspect]===value?undefined:value;renderAspects();update();}};
 $('review-rating').oninput=()=>{const n=$('review-rating').valueAsNumber;state.own=R.normalize({own:n}).own;update();};
 for(const id of ['dish','notes'])$('review-'+id).oninput=()=>{state[id]=$('review-'+id).value;update();};
 for(const id of ['return','length','tone'])$('review-'+id).onchange=()=>{state[id==='return'?'returnVisit':id]=$('review-'+id).value;update();};
 $('review-visited').onchange=()=>{state.visited=$('review-visited').checked;update(false);};
 $('draft-text').oninput=()=>{draft=$('draft-text').value;manual=true;state.visited=false;$('review-visited').checked=false;update(false);};
 $('review-rephrase').onclick=()=>{state.variant=(state.variant+1)%3;update();};
 $('draft-apply').onclick=()=>{if(manual&&!confirm('Deinen bearbeiteten Text durch die aktuell ausgewählten Bausteine ersetzen?'))return;manual=false;pending=false;update();};
 async function copy(openMaps){if($('copy-draft').disabled)return;draft=$('draft-text').value;storeDraft();await bridge('copy',{text:draft});if(openMaps)await bridge('open',{url:A.maps(venue)});else toast('Rezension kopiert');}
 $('copy-draft').onclick=()=>guarded(()=>copy(false));$('maps-draft').onclick=()=>guarded(()=>copy(true));
 $('draft-export').onclick=()=>guarded(()=>exportDraft({place:venue,state,text:draft,manual,updatedAt:new Date().toISOString()}));$('draft-list-back').onclick=showDrafts;
 $('draft-delete').onclick=()=>{if(!confirm('Diesen Rezensionsentwurf auf dem Gerät löschen?'))return;const all=readDraftRecords();delete all[venue.placeId];if(!placeService.write(DRAFTS,all))return toast('Entwurf konnte nicht gelöscht werden');renderDraftBadge();closeSheet();toast('Rezensionsentwurf gelöscht');};
 renderAspects();update(false);
}
